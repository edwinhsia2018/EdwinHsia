import React from "react";

const NoMatch = props => (
    <div>
        <h1>Whoops!  404 error. This page does not exist.</h1>
        <h1>Let's go <a href="/">HOME</a> instead and start over...</h1>
    </div>
)

export default NoMatch;
